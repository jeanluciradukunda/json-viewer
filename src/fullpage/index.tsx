import React from 'react';
import ReactDOM from 'react-dom/client';
import FullPage from './FullPageApp';
import '@/styles/globals.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <FullPage />
  </React.StrictMode>,
);
